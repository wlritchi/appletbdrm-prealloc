# Development convenience targets for this repo.
# Not shipped to /usr/src/ — Makefile is the kernel-module file used by DKMS.

.PHONY: clean

clean:
	rm -rf src/ pkg/
	rm -f -- *.pkg.tar.* *.log
	rm -f appletbdrm.c
